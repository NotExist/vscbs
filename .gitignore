# 鏡像下來的來源資料由 CI 產生並快取,不進版本庫(共約 62MB)
data/*.xml
data/archive/
data/mirror.json

# data/places.json 反而要進版本庫:它是地理編碼的種子,
# 少了它每次快取失效都得對 Nominatim 重查上千次。
!data/places.json

node_modules/
_site/
.DS_Store
